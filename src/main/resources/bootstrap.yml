spring:
    application:
        name: api-shop-manager
    profiles:
        active : dev
    cloud:
      config:
        enabled: true
        uri: http://localhost:8888/
        
server:
  port: 8086